# @oada/models

Shared OADA data model definitions.

This package provides model types and shared model-related code used by multiple
workspaces.

## Scripts

Run from `oada/`:

- `yarn workspace @oada/models run build`

## Notes

- `test` is currently a placeholder script in this package.
